- dconf settings for XYZ_name Gnome extension can be loaded with
`dconf load /org/gnome/shell/extensions/XYZ_name/ < XYZ_name.ini`

- dconf settings for XYZ_name Gnome extension can be dumped to an ini file with
`dconf dump /org/gnome/shell/extensions/XYZ_name/ > XYZ_name.ini`