---
- name: Test
  hosts: localhost
  vars_files:
    - ./playbook_vars/gnome_vars.yml

  tasks:
    - name: Install and set default cursor
      ansible.builtin.include_role:
        name: gnome
        tasks_from: cursor.yml
      vars:
        cursor: "{{ gnome_vars.desktop.cursor }}"
