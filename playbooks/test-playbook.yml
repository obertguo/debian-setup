---
- name: Test
  hosts: localhost
  vars_files:
    - ./playbook_vars/gnome_vars.yml

  tasks:
    - name: Configure Gnome extensions
      ansible.builtin.include_role:
        name: gnome
        tasks_from: configure_gnome_extensions
      loop: "{{ gnome_vars.gnome_extensions }}"
